# Versions

已发布功能的版本演进记录。

在此目录下记录已上线需求的后续迭代与版本变更。与 `02-requirements/` 的区别：

- `02-requirements/`：需求从 0 到 1 的设计过程
- `03-versions/`：已发布功能从 1 到 N 的迭代历史

## 命名规范

```
v{主版本}.{次版本}_{功能摘要}/
```

例如：`v1.2_搜索优化`、`v2.0_权限重构`
